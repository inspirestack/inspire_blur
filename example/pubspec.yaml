name: inspire_blur_example
description: A demo app showcasing the Inspire Blur.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  inspire_blur:
    path: ../

  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  icons_launcher: ^3.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
